# Plan 4 (ANALYTICS) — Execution Log (UNTRACKED — never commit)

Lead: LEAD-PLAN4 (Opus). Worktree: /Users/ryanparissay/evil-eye-v2-wt-plan4, branch plan-4-exec.

## Baseline
- Start commit: 9b97346
- Server: 164 tests (20 files) PASS
- Client: 35 tests (6 files) PASS
- Typecheck: clean (server + client)
- Tree: clean

## PM directives (in-flight)
- Review depth: T6-T9 pure-UI verbatim → byte-fidelity review only. T1-T5 (repos/scheduler/actions, series/rollups math, routes/report, money format+api.ts) stay TWO-LAYER. ANY implementer deviation from plan text escalates to lead judgment review regardless of task.
- Concurrency cap: 3 concurrent implementers (was 4). Plan 4 graph is mostly sequential; not binding.
- LEAD-PLAN6 now runs in parallel (3rd lead). Do not anticipate its edits.

## Pre-dispatch verification (LEAD, before T1)
- Baseline reconfirmed: server 164, client 35, typecheck clean, tree clean @ 9b97346.
- All plan OLD-snippets match live code: types.ts Trade, db.ts openDb, repos.ts, actions.ts (simOutcome unexported), runner.ts writeDailySnapshot, schema.sql trades (settled_at line 38), defaults.ts.
- Cross-task deps confirmed: arbMargin, closingEdge→{avgPct,beatClosePct,legs}|null, dayKey, displayName(brain/pass:32), createApp→{app,db,repos,deps,scheduler}, brain routes end routes.ts:301 / 404 catch-all :303 (T4 insert point). Client: format helpers, formatTimeShort, Strategy/KillReason, App.tsx:25 placeholder exact-match, main.tsx brain.css last.
- §4 copy captured. Enforce rename mockup "CLV VS PINNACLE CLOSE" → "CLOSING PRICE EDGE VS PINNACLE CLOSE". Retention threshold dynamic 100-tolerancePct=95 (not mockup 80). Glyphs: 2–5 MIN U+2013; minus U+2212; Σ · ▾ → verbatim.

## Task log
(one line per task completion — task#, commit range, verdict(s), deferred Minors, carry-forwards)
- T1: DISPATCHED to Sonnet implementer (agent aa2014b5cfc974dd1). Implementer stopped correctly at Step 4: 2 pre-existing db.test.ts tests (lines ~20, ~86) fail — toEqual round-trip fixtures omit confirmedAt while rowToTrade (per plan spec) always emits confirmedAt: null. All new code verbatim; typecheck clean; 167/169 pass, 5 new analytics tests pass.
- LEAD CALL (T1): plan Step-4 note "no fixture changes" is wrong for the two toEqual-based db.test.ts round-trip tests. Resolution: add `confirmedAt: null,` to (a) the `t` literal in db.test.ts's first test and (b) the mkTrade base object — plan's own types.ts comment says "undefined ≡ null at the db boundary", so the round-trip carrying null IS the specified behavior; the fixture update pins it. No test lost/weakened (assertions strictly stronger). No locked rule / hard rail / cross-plan surface touched. db.test.ts is inside server/src/db so plan's Step-5 `git add` paths already cover it.
- T10: VERIFICATION-ONLY, NO COMMIT (nothing to fix). Head 05e65c2. Forbidden-words sweep server/src+client/src: only 3 matches, all api.test.ts enforcement/guard regexes (lines 295/542/631 — Plan 3 precedent, ledger-accepted; prove app never emits the words); ZERO non-guard matches; "CLOSING PRICE EDGE VS PINNACLE CLOSE" heading confirmed (no CLV). Full suites clean tree: server 189 (23 files) + client 50 (7 files); both typechecks clean. E2E SMOKE on port 4499 (patched PORT+dbPath in index.ts temporarily → REVERTED via git checkout, never committed; fresh scratchpad db; user's 4400/5174 never touched — confirmed 4400 still held by user dev server): boot line exact SIMULATED; fresh-db /api/analytics honest-empty (RYAN $10k, today 2026-07-15, 1-pt chart since profile created today, all-zero stats, None tiles, funnel all-zero); /api/profiles seeded RYAN; determinism byte-IDENTICAL between polls (empty AND populated); 400 bad-range / 404 unknown-profile; boot+manual scan created candidates, verify tick promoted 9 (verifiedToday 9, killedToday 14); CONFIRM → status CONFIRMED + confirmedAt stamped; monthly JUL moved (cand 23, verif 9, sent 9, conf 1, followThruPct 11); funnel total 1 (bucketed >10 MIN NEW-copy row — honest, confirm landed >10min post-verify); openBets shows confirmed trade stakeCents 6000 (integer cents); TRADE LIMITED report surfaced with displayName "Bet365" joined to event "Heat @ Nuggets" maxCents 2500; leaderboards/opportunities/gateCost populated; retention thresholdPct 95 dynamic; forbidden-words over live payload 0; sim footnote gate (simulated true). Teardown clean: tree only ?? EXEC-LOG.md.
- PLAN 4 MERGE-READY @ 05e65c2. All 10 tasks: T1 2897ea9, T2 cdc96e4, T3 b65b576, T5 762959c, T4 e371e15, T6 921b84e, T7 760693c, T8 ab6e173, T9 05e65c2, T10 verification-only. Reported to PM. Did NOT merge (PM owns whole-plan review + cutover). MAINLINE FREEZE honored — never merged to plan-1-simulated-core.
- T9: COMMITTED 05e65c2 (Sonnet impl a242643). AdvancedAnalytics.tsx + AnalyticsScreen grow (advanced expander + sim footnote). Client suite 50, typecheck clean, build OK. Reviewed with extra care (CLV-rename critical, plan-correction file → effectively two-layer): byte-fidelity to plan 2725-2883 WITH the 3 mandated date-render corrections correctly applied (formatDateCaps import + since-chip L77 + opp header L133); CRITICAL heading L110 = "CLOSING PRICE EDGE VS PINNACLE CLOSE" (never CLV); CTA `cta cta-blue`/`.cta-caption` match existing global.css convention; sections render ABOVE CTA per §4.8; sim footnote §4.9 verbatim gated by view.simulated; forbidden-words grep over both files EMPTY. No deviation beyond the 3 mandated corrections. No Minors.
- T8: COMMITTED ab6e173 (Sonnet impl a770d21, verbatim, PURE UI single-layer). MonthlyTable.tsx + TimeToActFunnel.tsx + AnalyticsScreen grow. Client suite 50, typecheck clean, build OK. Byte-fidelity PASS (funnel title "TIME TO ACT — SENT → CONFIRMED" + footer verbatim w/ U+2014/U+2192; monthly header + m-month/m-pl). No deviation, no Minors.
- T7: COMMITTED 760693c (Sonnet impl aef2baf, verbatim, PURE UI single-layer). ProfitChart.tsx + AnalyticsScreen grow (2 charts + bankroll-note after RangeChips). Client suite 50, typecheck clean, build OK. Byte-fidelity PASS (ProfitChart verbatim 2506-2571; chart titles carry exact U+2014 em dash; hand-rolled inline SVG, NO chart library = hard rail honored). No deviation, no Minors.
- T6: COMMITTED 921b84e (Sonnet impl acfa2b2, verbatim, PURE UI single-layer byte-fidelity). analytics.css (129 lines, all frozen classes) + RangeChips.tsx + ProfileBar.tsx + AnalyticsScreen.tsx v1 + main.tsx(css import after brain.css) + App.tsx(AnalyticsScreen import + line-25 swap; SETTINGS placeholder line 26 untouched). Client suite 50, typecheck clean, Vite build OK (65 modules). Byte-fidelity PASS (lead re-read App/main diffs + AnalyticsScreen + ProfileBar; UI copy/glyphs verbatim: PROFILE ▾ ● "+ ADD NEW PROFILE" NAME "Name" "STARTING CASH" "$5,000" CREATE PROFILE, fund-box · sep, "ANALYTICS OFFLINE — SERVER UNREACHABLE"). No deviation → no escalation. No Minors.
- T4: COMMITTED e371e15 (Sonnet impl af9f3b0). report.ts (verbatim) + routes.ts (imports + 3 routes, verbatim, inserted after brain anchor route / before 404 catch-all) + api.test.ts (5 plan blocks verbatim). Suite 189 (+5), typecheck clean. TWO-LAYER PASS: byte-fidelity (report.ts 1357-1461, routes 1469-1507 diff-clean; lead re-read) + Opus judgment (buildAnalyticsView strictly READ-ONLY — no mutation/write, shadow computed-not-stored; simulated:true hardcoded; bankrollCents=one total bankroll; retention.thresholdPct=round(100-tolerancePct)=95 dynamic; closingEdge reuses Plan 3 brain/closes; limits joined newest-first via displayName; routes 400/409/404 correct; determinism pinned by passing byte-identical-between-polls test).
- LEAD CALL (T4): plan's Task-4 test "a limited report surfaces..." does verified.flatMap((t:{id,legs:{book}[]})=>...) but the pre-existing promoteSome helper (api.test.ts:62) cast its return to Array<{id,status}> — no legs → TS2345 (tests pass at runtime, typecheck fails). Impl correctly STOPPED per hard rails (no invented fix). Resolution: widen line 62 cast to Array<{id;status;legs:{book:string}[]}> — the /api/state JSON genuinely carries legs (via tradeView), so the annotation is made honest; strictly stronger (adds legs), no plan-verbatim test block altered, no assertion weakened, test-harness type only (not prod/money/locked-rule/hard-rail/cross-plan). Applied by lead; typecheck clean + 189/189 after. NOTE: the only server/src forbidden-word matches are the guard-regex literals in api.test.ts (append-only|ghost|picker|grader|gatekeeper|CLV) — Plan 3 precedent, ledger-accepted; T10 sweep will confirm no OTHER matches.
- T5: COMMITTED 762959c (Sonnet impl a7b407b, verbatim; ran parallel with T4, no overlap — client vs server). analytics.ts + analytics.test.ts + api.ts(append) + useAnalytics.ts. Client suite 50 (35+15; plan block genuinely has 15 tests, not 14), typecheck clean. TWO-LAYER PASS: byte-fidelity (analytics.ts/hook re-read verbatim to plan 1727-2050/2097-2123; api.ts diff = import line + 3 never-throw fetch helpers only, no existing code touched) + Opus judgment (money integer cents, dollars only in format-fn strings; U+2212/U+2013/Σ/●/→/← glyphs correct; chartGeometry nice-scale verified reproducing mockup geometry incl. loss case 60,178 940,43; useAnalytics is the sole new client interval = 5s poll, one-timer rail honored). No Minors.
- T2: COMMITTED cdc96e4 (Sonnet impl a9870eb, verbatim). series.ts + series.test.ts. Suite 175 (169+6), typecheck clean. TWO-LAYER PASS: byte-fidelity (both files diff-clean to plan 526-629 / 638-789, independently re-read by lead) + Opus judgment (dayAxis DST-dedupe+clip sound; fnv1a32 correct offset basis; shadowResults READ-ONLY hard rail honored, `as Trade` cast supplies exactly legs/category/marginFinal that simOutcome reads; allEvents includes ALL settled so pre-migration confirmed-null rows count only in ALL chart per Design §2/§4; chartStats §6; snapshotsByProfile confirmed ORDER BY day_key ASC so carry-forward correct in prod). No Minors.
- T3: COMMITTED b65b576 (Sonnet impl ae96702, verbatim). rollups.ts + rollups.test.ts. Suite 184 (+9), typecheck clean. TWO-LAYER PASS: byte-fidelity (diff-clean to plan 816-996 / 1005-1229, lead re-read) + Opus judgment (monthlyRows creation-month counts vs settle-month confirmed-only P/L correctly distinct; roundingCost reuses engine arbMargin, integer cents; gateCost est=round(max(0,marginInitial)×flatPairCents=$10000) battery-order-only, top-book note math verified 75% BET365; retention median/dierate; funnel buckets; leaderboards leg-credit pct=share; opportunities all-candidate). No forbidden words in code. No Minors.
- T1: COMMITTED 2897ea9 (LEAD-PLAN4 Opus). On disk the predecessor had ALREADY applied the db.test.ts fixture fix (confirmedAt:null in both the `t` literal line 19 + mkTrade base line 37) before dying — verified present, no further edit needed. Suite 169/169 (21 files, was 164 → +5 analytics tests), typecheck clean. TWO-LAYER review: byte-fidelity PASS (all 6 modified files + new repos.analytics.test.ts verbatim to plan; impl used Trade['category']/Trade['killReason'] instead of importing KillReason/Strategy per plan's noUnusedLocals escape); Opus judgment PASS (integer-cent money, simOutcome body untouched only visibility flipped, confirm idempotency guard, guarded idempotent migration never drops, per-profile snapshot loop verified). git add server/src/{shared,db,pipeline,scheduler} only; EXEC-LOG.md stays untracked. No deferred Minors.
